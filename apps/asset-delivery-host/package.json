{
  "name": "@prodivix/asset-delivery-host",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "pnpm run clean && tsc -p tsconfig.json",
    "clean": "node -e \"const fs=require('node:fs'); fs.rmSync('dist',{recursive:true,force:true}); fs.rmSync('tsconfig.tsbuildinfo',{force:true})\"",
    "dev": "tsx src/main.ts",
    "start": "node dist/main.js",
    "test:clamav-gate": "tsx scripts/verify-clamav-gate.ts",
    "test": "pnpm run typecheck && pnpm run typecheck:gate && vitest --config vitest.config.ts --run",
    "typecheck": "tsc -b tsconfig.test.json --pretty false",
    "typecheck:gate": "tsc -p tsconfig.gate.json --pretty false"
  },
  "dependencies": {
    "@prodivix/assets": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  }
}
