---
lastUpdated: false
---

# AST-3103 隔离交付响应 identity 漂移

## 快速信息

| 名称     | 说明       |
| -------- | ---------- |
| 前缀     | AST        |
| 范围     | 二进制资源 |
| 严重程度 | `error`    |
| 阶段     | `delivery` |
| 可重试   | 否         |

## 含义

AST-3103 表示 隔离交付响应 identity 漂移。请先确认当前页面、项目状态和最近操作，再按建议操作处理。

## 触发条件

Host 响应、capability URL、TTL 或 digest/media/dimension identity 漂移

## 建议操作

刷新页面重新获取交付地址；若仍失败，重新导入该资源

## 上报时提供

- 错误码和 requestId
- 当前项目或工作区 ID
- 触发该错误的操作
- 可复现时的最小文档或配置

[返回错误码索引](/reference/diagnostic-codes)
