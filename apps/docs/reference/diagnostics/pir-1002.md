---
lastUpdated: false
---

# PIR-1002 UI graph 缺失

## 快速信息

| 名称     | 说明     |
| -------- | -------- |
| 前缀     | PIR      |
| 范围     | PIR 文档 |
| 严重程度 | `error`  |
| 阶段     | `schema` |
| 可重试   | 否       |

## 含义

PIR-1002 表示 UI graph 缺失。请先确认当前页面、项目状态和最近操作，再按建议操作处理。

## 触发条件

PIR 文档缺少 `ui.graph`

## 建议操作

检查导入文件是否为支持的 PIR 文档

## 上报时提供

- 错误码和 requestId
- 当前项目或工作区 ID
- 触发该错误的操作
- 可复现时的最小文档或配置

[返回错误码索引](/reference/diagnostic-codes)
