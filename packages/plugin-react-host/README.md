# `@prodivix/plugin-react-host`

Stable privileged React projection ABI for build-attested official plugins.

The package contains React component, Palette, render-policy, icon, surface,
style, overlay, and Host Module contracts. Registry implementations, workspace
state, editor bridges, and package artifacts remain outside this package.
