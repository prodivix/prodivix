{
  "name": "@prodivix/animation",
  "version": "0.0.1",
  "type": "module",
  "description": "Transport-neutral animation contracts, normalization, authoring helpers, and evaluation for Prodivix",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./wire": {
      "types": "./dist/wire.d.ts",
      "import": "./dist/wire.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "lint": "eslint .",
    "build": "pnpm run clean && tsc -p tsconfig.json",
    "clean": "node -e \"const fs=require('node:fs'); fs.rmSync('dist',{recursive:true,force:true}); fs.rmSync('tsconfig.tsbuildinfo',{force:true})\"",
    "test": "pnpm run typecheck && vitest --config vitest.config.ts --run",
    "test:watch": "vitest --config vitest.config.ts",
    "typecheck": "tsc -b tsconfig.test.json --pretty false"
  },
  "dependencies": {
    "@noble/hashes": "2.2.0",
    "@prodivix/authoring": "workspace:*",
    "@prodivix/behavior": "workspace:*",
    "@prodivix/runtime-core": "workspace:*",
    "@prodivix/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "fast-check": "^4.9.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  }
}
