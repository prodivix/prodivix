{
  "name": "@prodivix/plugin-browser",
  "version": "0.0.1",
  "type": "module",
  "description": "Opaque iframe and Dedicated Worker runtime adapter for Prodivix plugins",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "imports": {
    "#browser/*": {
      "types": "./dist/*.d.ts",
      "import": "./dist/*.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "sideEffects": false,
  "scripts": {
    "lint": "eslint .",
    "build": "pnpm run clean && pnpm run generate && tsc -p tsconfig.json",
    "check:generated": "node scripts/generate-worker-bootstrap.mjs --check",
    "clean": "node -e \"const fs=require('node:fs'); fs.rmSync('dist',{recursive:true,force:true}); fs.rmSync('tsconfig.tsbuildinfo',{force:true})\"",
    "generate": "node scripts/generate-worker-bootstrap.mjs",
    "test": "pnpm run check:generated && pnpm run typecheck && vitest --config vitest.config.ts --run",
    "test:watch": "vitest --config vitest.config.ts",
    "typecheck": "tsc -b tsconfig.test.json --pretty false"
  },
  "dependencies": {
    "@prodivix/plugin-contracts": "workspace:*",
    "@prodivix/plugin-host": "workspace:*",
    "@prodivix/plugin-protocol": "workspace:*",
    "@prodivix/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "fake-indexeddb": "^6.2.5",
    "prettier": "^3.9.4",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  }
}
