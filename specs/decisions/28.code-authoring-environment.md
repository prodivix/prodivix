# Code Authoring Environment

## 状态

- Accepted
- 日期：2026-05-17
- 关联：
  - `specs/decisions/25.authoring-symbol-environment.md`
  - `specs/decisions/27.diagnostic-presentation-contract.md`
  - `specs/decisions/21.inspector-panel-architecture.md`
  - `specs/decisions/20.node-graph-port-semantics.md`
  - `specs/decisions/22.llm-integration-architecture.md`
  - `specs/decisions/17.external-library-runtime-and-adapter.md`
  - `specs/decisions/05.workspace-vfs.md`
  - `specs/workspace/workspace-model.md`
  - `specs/implementation/authoring-symbol-environment-phase1.md`
  - `specs/implementation/authoring-environment-stable-structures.md`
  - `specs/implementation/code-authoring-environment-phase2.md`

## 决策背景

MFE 的核心产品形态是三编辑器架构：

1. Blueprint 负责 UI graph、布局、组件属性、事件入口和样式挂载。
2. NodeGraph 负责逻辑图、端口、连线、执行语义和自定义执行函数。
3. Animation 负责 timeline、track、binding、filter 和关键帧。

但前端项目中存在大量 **code-owned** 内容：它们不适合被普通 PIR 结构完整表达，也不应被三编辑器强行可视化。例如：

- 复杂 event handler、表单校验、数据转换和业务服务。
- NodeGraph 自定义 executor、条件函数和外部运行时代码。
- Animation easing、timeline script、GLSL / WGSL、复杂动效函数。
- React Flow、CodeMirror、Monaco、Three.js、图表、富文本等复杂库的 adapter。
- Workspace 中的普通 TS/TSX/CSS/JSON/Markdown 文件。

现有架构图中已经有 Code Editor / IntelliSense，但它更像三编辑器的附属能力，没有明确说明 code-owned 内容的长期归属。若没有一等作者环境，MFE 会在两种错误之间摇摆：

1. 过度承诺，把所有库和代码都强行可视化，导致 PIR 变成不可维护的通用编程语言。
2. 完全放弃 code-owned 内容，只把复杂能力当成黑盒，导致可视化编辑、诊断、AI 和代码之间断裂。

因此需要把代码作者态定义成三编辑器共享的基础设施，而不是第四个互不相干的业务编辑器。

## 决策目标

1. 明确 MFE 仍是三编辑器可视化架构，但 code-owned 内容由共享 Code Authoring Environment 承载。
2. 让代码成为 MFE 项目模型中的一等公民：可编辑、可引用、可诊断、可预览、可被 AI 修改。
3. 保持 PIR 的边界：PIR 引用代码，但不吞并代码源码和复杂库内核。
4. 为 Blueprint、NodeGraph、Animation、Inspector、Resources、AI 和 Issues 面板提供统一代码入口。
5. 让 `Authoring Symbol Environment` 成为 Code Authoring Environment 的符号、作用域和诊断底座。

## 核心决策

### 1) Code Authoring Environment 是共享底座，不是第四编辑器

MFE 保持三编辑器架构：

```text
Blueprint Editor
NodeGraph Editor
Animation Editor
```

Code Authoring Environment 作为三者共同调用的代码作者态底座：

```text
Blueprint / NodeGraph / Animation / Inspector / Resources / AI / Issues
    -> Code Authoring Environment
    -> Workspace VFS
    -> CodeArtifact / CodeSymbol / CodeScope / Diagnostics
```

规则：

1. 不把 Code Authoring Environment 描述为第四个并列业务编辑器。
2. 三编辑器遇到 code-owned 边界时，通过统一入口打开代码工作区、代码片段或符号选择器。
3. Code Authoring Environment 不直接拥有 Blueprint、NodeGraph 或 Animation 的领域语义；领域语义由对应编辑器或 provider 提供。
4. Code Authoring Environment 负责代码文件、代码片段、语言服务、符号、引用、诊断和定位体验。

### 2) code-owned 不等于黑盒放弃

code-owned 表示代码是事实源，但 MFE 仍提供作者态能力。

| 能力 | Code Authoring Environment 职责                           |
| ---- | --------------------------------------------------------- |
| 编辑 | 文件树、tabs、dirty state、Monaco / CodeMirror、保存      |
| 引用 | `CodeReference` 指向 artifact、export、symbol 或 span     |
| 诊断 | TypeScript、lint、adapter contract、PIR 引用断裂、runtime |
| 定位 | 从 PIR 节点跳到代码，从诊断跳到 source span               |
| 预览 | 运行时 preview、sandbox、错误边界                         |
| AI   | 基于上下文生成、解释、修复、重构代码 patch                |
| 集成 | 绑定 event handler、executor、adapter、easing、CSS        |

规则：

1. 对 code-owned 内容，MFE 不承诺完整可视化编辑内部实现。
2. MFE 必须能管理 code-owned 内容的入口、引用、诊断和生命周期。
3. 代码必须进入 Workspace VFS 或稳定 CodeArtifact，而不是散落在 UI 局部状态中。
4. 代码能力必须能被 Issues 面板、AI 和相关编辑器定位。

### 3) Workspace VFS 是作者态文件系统

Workspace VFS 不只是 PIR 多文档容器。它是 MFE 项目的作者态文件系统，负责承载 PIR 文档、NodeGraph 文档、Animation 文档、代码文件、样式、shader、adapter 和普通 workspace module。

长期形态：

```text
/
├─ pages/
│  └─ home.pir.json
├─ components/
│  └─ dialog.pir.json
├─ nodegraphs/
│  └─ fetch-user.graph.json
├─ animations/
│  └─ intro.animation.json
├─ src/
│  ├─ actions/openDialog.ts
│  └─ node-executors/fetchUser.ts
├─ styles/
│  └─ home.mounted.css
└─ shaders/
   └─ wave.glsl
```

规则：

1. VFS 路径直接等于用户代码路径和作者心智路径，例如 `/src/actions/openDialog.ts`。
2. `.mfe/...` 可以保存 workspace manifest、route manifest 和内部索引，但不吞并用户代码路径模型。
3. `WorkspaceDocument.id` 是稳定身份，不随移动、重命名或路径调整改变。
4. `WorkspaceDocument.path` 是用户可理解的路径，用于文件树展示、Git 导出和错误文案。
5. PIR、NodeGraph、Animation 和 code document 在 VFS 中并行存在，但事实源边界不同。
6. PIR / NodeGraph / Animation 负责可视化结构事实源；code document 负责 code-owned 内容事实源。
7. Code Authoring Environment 消费 VFS 中的 code document，并将其投影为 `CodeArtifact`。

Phase 2 必须让后端 workspace 支持 `code` document。前端类型中已有 `code` 文档和 `code` command domain 的方向，但后端 `WorkspaceDocumentType`、存储校验、PATCH 策略和 projection 需要补齐。

### 4) CodeArtifact 是 Workspace code document 的作者态投影

CodeArtifact 不应与 WorkspaceDocument 竞争事实源。对于 VFS 中的代码文件，Phase 2 默认采用：

```text
CodeArtifact.id = WorkspaceDocument.id
```

```ts
type CodeArtifact = {
  id: WorkspaceDocumentId;
  path: string;
  language: CodeArtifactLanguage;
  owner: CodeArtifactOwner;
  source: string;
  revision: string;
};
```

规则：

1. `id` 用于稳定引用、诊断、历史和重命名安全。
2. `path` 用于用户心智、文件树、导出和显示，不作为稳定主键。
3. 一个 workspace code document 在 Phase 2 先投影为一个 CodeArtifact。
4. 未来可以支持一个 document 内多个 artifact，但不得改变 `documentId` 作为稳定身份的基本原则。
5. `revision` 来源于 workspace document 的 content revision 或等价版本，不规定具体生成算法。

### 5) CodeReference 是持久化代码引用

PIR、NodeGraph、Animation 和 TriggerBinding 中的代码引用必须使用稳定身份，不依赖路径或临时名称。

长期模型：

```ts
type CodeReference = {
  artifactId: string;
  exportName?: string;
  symbolName?: string;
  sourceSpan?: SourceSpan;
};
```

规则：

1. `artifactId` 是主引用，Phase 2 默认等于 `WorkspaceDocument.id`。
2. `exportName` 适用于 TS / JS module export。
3. `symbolName` 适用于 expression、CSS symbol、shader entry、adapter entry 等非标准 export 场景。
4. `sourceSpan` 只用于定位辅助，不作为唯一身份。
5. `path` 可以作为显示和恢复线索，但不得成为持久化引用主键。
6. `{ name, scopeId }` 这类轻引用可以作为 resolver 查询输入，但不作为 PIR / slot / trigger 的长期持久化模型。

CodeReference 断裂、签名不匹配、作用域不可见等问题使用 `COD-xxxx` 诊断。

### 6) Code slots 是连接代码能力的稳定边界

三编辑器不应直接保存任意代码字符串。它们通过明确 code slot 声明“这里可以接入代码能力”。Code slot 是能力声明，不是代码内容，也不是事件动作本身。

规则：

1. 每个 code slot 必须声明 owner、输入、输出、能力约束和诊断落点。
2. slot 的绑定值应是 `CodeReference`，不是裸字符串。
3. slot contract 不保存代码源码，也不负责保存所有绑定状态。
4. slot 的签名校验属于作者态诊断，通常使用 `COD-xxxx`。
5. slot owner 删除后，相关 CodeArtifact 默认标记为 orphan，不自动删除。
6. orphan artifact 应保留在 VFS 中，并提供删除、重新绑定或转为 workspace module 的用户动作。

示例：

```ts
type CodeSlotContract = {
  id: string;
  ownerRef: DiagnosticTargetRef;
  kind:
    | 'event-handler'
    | 'validator'
    | 'node-executor'
    | 'animation-function'
    | 'external-adapter'
    | 'mounted-css'
    | 'workspace-module';
  inputTypeRef?: string;
  outputTypeRef?: string;
  capabilityIds: string[];
  defaultPlacement: DiagnosticPlacement[];
};
```

`DiagnosticPlacement` 使用诊断展示契约中的落点模型，不在 code slot 内另起一套展示协议。

### 7) CodeSlotRegistry 只索引 slot，不拥有代码和绑定

CodeSlotRegistry 是 Code Authoring Environment 的聚合入口，用于查询当前上下文有哪些代码插槽。

```ts
type CodeSlotProvider = {
  id: string;
  source: SymbolSource;
  listSlots(context: AuthoringContext): CodeSlotContract[];
  getSlot(id: string): CodeSlotContract | null;
};

type CodeSlotRegistry = {
  register(provider: CodeSlotProvider): void;
  unregister(providerId: string): void;
  listSlots(context: AuthoringContext): CodeSlotContract[];
  getSlot(id: string): CodeSlotContract | null;
  listSlotsByOwner(ownerRef: DiagnosticTargetRef): CodeSlotContract[];
};
```

分工：

| 模块                             | 职责                                            |
| -------------------------------- | ----------------------------------------------- |
| CodeSlotRegistry                 | 哪里可以接入代码                                |
| CodeArtifactProviderRegistry     | 代码实际在哪里                                  |
| Authoring Symbol Environment     | 代码里有哪些 symbol、引用能否解析、诊断落在哪里 |
| PIR / NodeGraph / Animation 文档 | 保存领域结构和 slot binding                     |

规则：

1. Registry 通过 provider 聚合 slot，不直接扫描编辑器内部 store 或 UI 结构。
2. Registry 不保存源码。
3. Registry 不集中保存所有 binding；binding 由对应领域文档保存。
4. Registry 可以按 owner、surface、capability 查询 slot，用于 Inspector、Issues、AI 和 Code Editor 入口。
5. CodeSlotProvider 可以随模块内部重构而替换，只要输出的 `CodeSlotContract` 稳定。

绑定示例：

```ts
type CodeSlotBinding = {
  slotId: string;
  reference: CodeReference;
};
```

`CodeSlotBinding` 通常保存在对应领域文档中，例如 PIR node event、NodeGraph executor field、Animation binding field 或外部 adapter 配置。它不是 registry 的全局状态。

### 8) Trigger / Action 不等于 CodeSlot

Trigger 表示入口，Action 表示事件要做什么，CodeSlot 只表示触达代码能力时的边界。MFE 不应把所有 trigger 都降级成 JavaScript handler。

```ts
type TriggerBinding =
  | { kind: 'open-url'; href: string }
  | { kind: 'navigate-route'; routeId: string }
  | { kind: 'run-nodegraph'; graphId: string; inputMapping?: unknown }
  | {
      kind: 'play-animation';
      timelineId: string;
      command: 'play' | 'pause' | 'seek';
    }
  | { kind: 'call-code'; slotId: string; reference: CodeReference };
```

规则：

1. 打开链接、路由跳转、执行节点图、播放动画等非代码 trigger 继续作为结构化 action 存在。
2. 只有触达 event handler、executor、adapter、mounted CSS、shader、timeline script 等 code-owned 内容时才进入 CodeSlot / CodeReference。
3. NodeGraph 和 Animation 仍是可视化运行时；其中的复杂 executor、transform、easing、shader 等代码叶子节点才属于 code-owned。
4. AI 生成行为时应优先使用结构化 action；只有确实需要代码能力时才创建或绑定 CodeReference。

### 9) Authoring Symbol Environment 是 Code Authoring Environment 的索引层

`specs/decisions/25.authoring-symbol-environment.md` 定义的 Authoring Environment、CodeArtifact、CodeSymbol、CodeScope、TargetRef 和 SourceSpan 继续保持稳定。

本决策补充其架构归属：

```text
Code Authoring Environment
├─ Workspace Code Editor
├─ Code Slot Registry
├─ Language Service Adapter
├─ Authoring Symbol Environment
│  ├─ CodeArtifact Provider Registry
│  ├─ CodeSymbol Provider Registry
│  ├─ Authoring Diagnostic Provider Registry
│  └─ Reference / Definition / Completion Query API
├─ Diagnostic Bridge
└─ AI Code Patch Bridge
```

规则：

1. Authoring Symbol Environment 不单独承担完整 IDE 体验。
2. 它负责共享符号、作用域、引用、补全和诊断查询。
3. Code Authoring Environment 负责编辑器外壳、文件体验、语言服务接入、代码 slot 生命周期和用户交互。
4. 三编辑器消费符号环境时，应通过 Code Authoring Environment 或其稳定查询接口，不直接扫描其他编辑器内部结构。

### 10) 复杂库按能力等级接入，不逐库承诺完整可视化

MFE 对外部库采用能力分级：

| 等级      | 含义                                                        |
| --------- | ----------------------------------------------------------- |
| Native    | MFE 内置能力，完整 Inspector / PIR / preview / codegen 支持 |
| Adapted   | 有 adapter，支持主要 props、slots、状态和 code slot         |
| Embedded  | 可运行和预览，但内部实现作为黑盒                            |
| Code-only | 只能在代码中使用，MFE 只提供依赖、文件、诊断和构建层面支持  |

对 React Flow 这类复杂领域库，合理形态是：

```text
Embedded graph engine + Adapted node-card surface + Code-owned graph semantics
```

规则：

1. MFE 不追求可视化编辑所有前端库。
2. 对复杂领域引擎，优先适配能力类型，而不是适配库海。
3. adapter 可以暴露可视化 surface，例如 React Flow 的节点卡片模板。
4. 引擎内核、连接规则、性能边界、私有状态仍由代码拥有。

### 11) PIR 引用代码，不吞并代码

PIR 保存态不应内联完整代码文件或复杂库内部状态。PIR 只保存结构化引用。

规则：

1. PIR 中的 event、binding、adapter、executor、animation hook 应引用 `CodeReference`。
2. 代码源码保存在 Workspace VFS 的 code document 中，并由 CodeArtifact 投影给作者态环境。
3. CodeReference 断裂、签名不匹配、作用域不可见等问题使用 `COD-xxxx` 诊断。
4. Codegen 可以读取 CodeReference 生成目标框架代码，但不改变作者态代码归属。

## 典型场景

### Blueprint event handler

```text
Button node
  onClick -> TriggerBinding(call-code)
    slotId: blueprint.button-1.onClick
    reference: CodeReference(artifactId=code_open_dialog, exportName=openDialog)
```

流程：

1. 用户在 Inspector 中点击 `Create handler` 或 `Open handler`。
2. Code Authoring Environment 打开对应 artifact。
3. Authoring Symbol Environment 检查 handler 是否存在、是否在当前 scope 可见、签名是否匹配。
4. 诊断可以落到 Inspector 字段、Code Editor inline 或 Issues 面板。

### NodeGraph custom executor

```text
Custom Node
  executor -> CodeReference(artifactId=code_fetch_user_executor, exportName=execute)
```

流程：

1. NodeGraph 只保存 executor 引用和端口契约。
2. executor 源码由 Code Authoring Environment 编辑。
3. NodeGraph provider 暴露 graph input / output / port scope。
4. signature mismatch 进入 `COD-2003` 或后续更具体码位。

### React Flow node card template

```text
React Flow NodeShell
  -> code-owned handles / selection / graph events
  -> MFE-authored NodeCardTemplate
```

规则：

1. NodeShell 由 adapter 代码拥有。
2. NodeCardTemplate 可以是 PIR fragment 或生成的 React component。
3. React Flow `Handle`、edge validation、node id 和 graph events 不由模板任意创建。
4. 模板只能消费只读 scope 和受控 slot。

### 非代码 trigger

```text
Button node
  onClick -> TriggerBinding(open-url)
    href: https://example.com

Button node
  onClick -> TriggerBinding(run-nodegraph)
    graphId: graph_fetch_user

Button node
  onClick -> TriggerBinding(play-animation)
    timelineId: timeline_intro
    command: play
```

规则：

1. 这些 trigger 不需要 CodeSlot。
2. 它们仍应进入 PIR / NodeGraph / Animation 的结构化 action 模型。
3. 当 action 的某个叶子能力需要用户代码时，才通过 CodeSlot 连接 CodeReference。

## 直接影响

### 架构表述

项目架构应从“代码编辑器是三编辑器旁边的附属能力”调整为：

```text
三编辑器 + 共享 Code Authoring Environment
```

其中三编辑器仍是主要产品面，Code Authoring Environment 是跨编辑器基础设施。

### Inspector

Code Tab 不应只是 triggers 的容器。它应成为当前节点相关 code slots 和 code references 的入口，例如：

- event handlers
- mounted CSS
- external adapter hooks
- custom expressions

### NodeGraph

NodeGraph 不直接内联复杂 executor 源码。它通过 code slot 绑定 executor，并把 port / graph scope 暴露给 Authoring Symbol Environment。

### Animation

Animation 不把所有复杂 easing、shader、timeline script 都塞入可视化 timeline。复杂逻辑通过 code slot 进入 Code Authoring Environment。

### AI

AI 生成代码时不直接改任意文件。它应基于 code slot、CodeArtifact、Workspace VFS 和 Authoring Environment 生成可审阅 patch。

### Backend Workspace

后端 workspace 必须支持 code document，而不是只支持 PIR / NodeGraph / Animation 文档。

要求：

1. 增加 `WorkspaceDocumentTypeCode`。
2. 定义 code document 的内容模型、校验策略和 PATCH 策略。
3. 支持 VFS 路径作为用户代码路径，例如 `/src/actions/openDialog.ts`。
4. 保持 `WorkspaceDocument.id` 作为稳定身份，不随 path 改变。
5. 与 `CodeArtifact.id = WorkspaceDocument.id` 的 Phase 2 约定兼容。

短期若后端仍以 `jsonb` 保存 document content，code document 可以使用 JSON wrapper：

```ts
type WorkspaceCodeDocumentContent = {
  language: CodeArtifactLanguage;
  source: string;
  metadata?: Record<string, unknown>;
};
```

是否支持纯文本内容可以后续演进，但不得破坏 `WorkspaceDocument.id`、`path` 和 CodeArtifact 投影语义。

## 当前实现状态与缺口

当前已有基础：

1. 前端 Workspace 类型已包含 `code` document。
2. 前端 workspace command 已包含 `domainHint: 'code'`，并允许 code document patch `/content`。
3. Authoring Environment 已有 `CodeArtifact`、`CodeSymbol`、`CodeScope`、`DiagnosticTargetRef`、`SourceSpan` 和 provider registry 基础。

当前缺口：

1. 后端 `WorkspaceDocumentType` 还未包含 `code`。
2. 后端 workspace document 内容仍按 JSON 文档处理，还没有 code document 的明确内容模型。
3. Workspace projection 当前偏内部 `.mfe/documents/...` 投影，还未形成“VFS path 直接等于用户代码路径”的导出/导入约束。
4. 当前 `CodeReference` 实现仍偏轻量查询形态，尚未升级为以 `artifactId` 为核心的持久化引用。
5. 尚未实现 CodeSlotContract、CodeSlotProvider 和 CodeSlotRegistry。
6. 尚未定义 TriggerBinding / ActionContract 的正式落地位置。

## 分阶段落地

### Phase 1：文档和边界

1. 明确三编辑器 + 共享代码作者环境的架构表述。
2. 更新 `specs/decisions/25.authoring-symbol-environment.md`，说明其归属于 Code Authoring Environment。
3. 保持现有 Phase 1 Authoring Symbol Environment 契约不变。

### Phase 2：Code slot registry

1. 后端和前端 workspace 支持 `code` document。
2. 约定 VFS path 直接等于用户代码路径。
3. 将 workspace code document 投影为 CodeArtifact。
4. 将持久化 CodeReference 升级为 `artifactId` 主导模型。
5. 定义 `CodeSlotContract`、`CodeSlotProvider` 和 `CodeSlotRegistry` 轻类型。
6. 定义 TriggerBinding / ActionContract 与 CodeSlot 的边界。
7. 不急于确定首批具体 slot；slot 清单应在 implementation spec 中按业务入口分批落地。

### Phase 3：Workspace code editor integration

1. 将 Workspace VFS 中的代码文件和 CodeArtifact 统一接入代码工作区。
2. 支持从 Inspector / NodeGraph / Animation / Issues 跳转到 code artifact。
3. 接入最小 language service adapter 和 inline diagnostic adapter。

### Phase 4：Domain adapters

1. 选择少数复杂领域库做标杆 adapter。
2. React Flow 可先落地 NodeShell + NodeCardTemplate 边界。
3. CodeMirror / Monaco 作为 code editor engine，保持 embedded / code-owned。

## 长期不变量

1. MFE 不把所有代码和复杂库内核强行 PIR 化。
2. Code-owned 内容必须可被 MFE 编辑、引用、诊断和定位。
3. Workspace VFS 是作者态文件系统，VFS path 直接等于用户代码路径。
4. `WorkspaceDocument.id` 是稳定身份，`WorkspaceDocument.path` 是用户路径。
5. CodeArtifact 是 workspace code document 的作者态投影。
6. CodeReference 以 artifact identity 为核心，不以 path 或临时 name 为主键。
7. 三编辑器通过 code slot 与代码连接，不直接保存任意代码字符串。
8. Trigger / Action 不等于 CodeSlot；非代码 action 继续结构化存在。
9. owner 删除后 code artifact 默认 orphan，不自动删除。
10. Authoring Symbol Environment 是共享索引层，不是完整 IDE 外壳。
11. adapter 暴露可视化 surface，但不泄漏复杂引擎私有状态。

## 非目标

1. 本决策不要求立即实现完整 IDE。
2. 本决策不要求把 Code Editor 作为第四个并列业务编辑器。
3. 本决策不要求立刻接入完整 TypeScript language service。
4. 本决策不要求深度适配所有外部库。
5. 本决策不改变 PIR v1.3 保存态结构。
6. 本决策不定义目标框架最终代码文件布局。
7. 本决策不要求立即确定首批具体 code slot 清单。
8. 本决策不要求一次性实现完整文件系统 API、CRDT 或二进制资产管线。

## 验收标准（Draft）

- [x] 架构文档明确 “三编辑器 + 共享 Code Authoring Environment”。
- [x] `Authoring Symbol Environment` 被定义为 Code Authoring Environment 的符号和诊断底座。
- [x] code-owned 内容有明确分级和非目标声明。
- [x] Workspace VFS 被定义为 PIR、code、style、shader、adapter 并行存在的作者态文件系统。
- [x] `WorkspaceDocument.id` 与 `WorkspaceDocument.path` 的长期分工明确。
- [x] CodeReference 采用 `artifactId` 主导的持久化模型。
- [x] CodeSlotRegistry 与 CodeArtifactProviderRegistry、Authoring Symbol Environment 的分工明确。
- [x] Trigger / Action 与 CodeSlot 的边界明确。
- [x] owner 删除后的默认策略是 orphan artifact。
- [x] PIR 继续只保存代码引用，不保存任意完整源码。
- [x] React Flow 等复杂库的可视化边界可用 Embedded / Adapted / Code-owned 分层解释。
