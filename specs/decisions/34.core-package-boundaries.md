# Core Package Boundaries

## 状态

- Accepted
- 日期：2026-07-12
- 实现状态：Phase 1 + Workspace Sync Core Implemented
- 关联 Hard Cut：`specs/decisions/35.canonical-workspace-hard-cut.md`

## 决策背景

Workspace、PIR、Diagnostics 与 Code Authoring 已经成为 Web Editor、Compiler、CLI、VS Code、后端协议和未来 Agent Runtime 的共同基础。这些领域实现继续位于 `apps/web/src` 会造成三个问题：

1. Web 应用成为领域核心的隐式 owner，其他入口只能复制实现或反向依赖应用。
2. React、Zustand、DOM 与 Vite 依赖容易渗入可序列化、可重放的作者态内核。
3. Preview、Compiler 与 Generated Runtime 会各自实现相同语义；此前 Compiler 与 Web 已经存在两份 `materializeUiTree`。

因此，`apps/web` 必须收敛为 Web 产品的 composition root 与交互适配层；稳定领域语义必须由独立 package 持有。

## 依赖方向

依赖箭头从消费者指向被依赖方：

```mermaid
flowchart TD
    Web[apps/web] --> Workspace[@prodivix/workspace]
    Web --> WorkspaceSync[@prodivix/workspace-sync]
    Web --> Authoring[@prodivix/authoring]
    Web --> PIR[@prodivix/pir]
    Web --> Diagnostics[@prodivix/diagnostics]

    Compiler[@prodivix/prodivix-compiler] --> PIR

    WorkspaceSync --> Workspace
    Workspace --> Authoring
    Authoring --> Diagnostics
    PIR --> Diagnostics

    Workspace --> Shared[@prodivix/shared subpath contracts]
    Authoring --> Shared
    PIR --> Shared
```

`@prodivix/shared` 当前仍提供 PIR、Router 与基础代码类型的 contract subpath；它不是新的运行时聚合包。后续应继续把 PIR Schema/generated types 与 Router 分别收敛到领域 package，最终缩小 Shared。

## Phase 1 包边界

### `@prodivix/diagnostics`

拥有：

- 可序列化诊断 envelope；
- definition/presentation contract；
- 通用 action、evidence 与类型守卫。

不拥有：

- PIR、Code、UX 等所有领域 catalog 的全局聚合；
- React Issues UI、Web navigation 或文档路由解析。

PIR catalog 归 `@prodivix/pir`，Code catalog 归 `@prodivix/authoring`。现有 UX catalog 仅通过 `@prodivix/diagnostics/ux` opt-in subpath 暴露，待 Quality domain 建立后再迁移。Web composition root 负责把领域 provider 聚合为产品 Issues 视图。

### `@prodivix/authoring`

拥有：

- `CodeArtifact`、`CodeReference`、`CodeSymbol`、`CodeScope`；
- `CodeSlot` 与 Trigger binding contract；
- artifact/symbol/diagnostic/slot provider registries；
- `AuthoringEnvironment` composition。

不拥有 Workspace snapshot adapter、Route manifest adapter、CodeMirror、Monaco 或 React 状态。

### `@prodivix/pir`

拥有：

- UiGraph normalize/materialize/mutation；
- ValueRef 求值；
- PIR normalization 与 validation；
- PIR diagnostic catalog。

不拥有 React Renderer、组件注册、图标加载、DOM selection、浏览器 navigation 或 route debug。

Compiler 必须消费该包的 materializer，不再维护第二份实现。

### `@prodivix/workspace`

拥有：

- Workspace VFS 与 snapshot contract；
- Command/Intent、Patch policy 与 History；
- validation、selectors 与 `.prodivix/**` projection；
- Workspace code document 到 CodeArtifact 的 authoring bridge。

不拥有 Zustand、fetch、IndexedDB、React hook 或 Web API client。

### `@prodivix/workspace-sync`

拥有：

- Workspace/Route/Document revision capture 与 canonical 409 decoder；
- transport-neutral Workspace semantic diff、code text diff 与三方合并；
- conflict session、显式 resolution 与 fresh WorkspaceOperation 构建；
- 从 WorkspaceOperation 写集推导 exact Atomic Commit revision vector；
- 后续 durable outbox 的状态机与因果语义。

不拥有 fetch、认证 token、Zustand、React conflict UI 或具体后端 endpoint adapter。Web 负责 transport 与 presentation composition，但不得在 `apps/web` 复制 diff3/rebase 语义。

### `apps/web`

继续拥有：

- React 页面、画布、Inspector 与 Store adapter；
- Web navigation、DOM、localStorage/IndexedDB 与 API transport；
- PIR React Renderer 的当前实现；
- Route runtime CodeSlot 的 composition adapter。

## 强制约束

Phase 1 core package 必须满足：

1. 生产源码不得导入 `@/`、`apps/web`、React、Zustand、React Flow 或 `@prodivix/ui`。
2. TypeScript 使用 `lib: ["ES2022"]`，不通过 DOM lib 隐式获得浏览器全局。
3. package 可以独立 build 和 test；测试从公共 barrel 或包内稳定入口消费。
4. Domain catalog 由领域 owner 持有，基础 Diagnostics 不反向聚合领域。
5. Web、Compiler 与未来 Generated Runtime 的相同语义应复用同一实现或同一 conformance suite。
6. 项目处于 alpha，不保留旧 `apps/web/src/{workspace,authoring,diagnostics}` alias 或兼容转发层。

边界由 `scripts/check-core-package-boundaries.mjs` 检查，并接入根 `pnpm lint`。

## Phase 1 后续收口

ADR 35 已完成 Web Workspace Hard Cut：Store 只持有一个 canonical snapshot，wire DTO 只在 API/IndexedDB 边界存在，PIR/Route/NodeGraph/Animation 不再保留 authoring mirror 或 localStorage 事实源。`@prodivix/workspace-sync` 也已承接 semantic diff3、conflict session、resolution operation 与 Atomic Commit planner，不再由 Web 持有同步核心语义。

Core package 的下一步不再是继续搬运 Web 兼容代码，而是建立缺失的领域 owner 与跨端能力：Router 独立 owner、Workspace Sync durable outbox、Animation evaluator 和 Runtime ports。

## 后续抽取批次

1. `@prodivix/router`：从 Shared 与 Web Store 中收敛 route contract、matcher、mutation、validation、intent planner 与 authoring adapter。
2. `@prodivix/workspace-sync` 后续：在已完成的 semantic recovery core、Atomic Commit planner 与后端 write-set/CAS conformance 上补 durable outbox、ACK causality 和跨刷新恢复，不回引 PIR fallback 或旧 `/batch`。
3. `@prodivix/animation`：统一 Web preview 与 Compiler 的 timeline/keyframe 求值语义。
4. `@prodivix/runtime-core` 与 `@prodivix/nodegraph`：移除 React Flow、Window、CustomEvent 和全局 singleton。
5. `@prodivix/pir-react-renderer` 与 `@prodivix/runtime-browser`：在 action/navigation/diagnostic/component ports 稳定后抽取 React 与浏览器投影。
6. 缩小 `@prodivix/shared`：PIR generated contract、Router 与 LLM runtime 分别归还领域 owner。

## Phase 1 验收

- 五个核心 package（含 `@prodivix/workspace-sync`）可独立 build/test。
- Web 只通过 `@prodivix/*` public API 消费已迁移核心。
- Compiler 已删除重复 PIR materializer。
- 根 typecheck、unit tests、lint 与 production build 全部通过。
